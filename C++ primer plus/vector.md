# vector

## 初始化

```c++
vector<T> vec;
vector<T> vec2(vec);
vector<T> vec3 = vec;
vector<T> vec4(n, val);
vector<T> vec5(n);
vector<T> vec6{a, b, c, ...};
vector<T> vec7 = {a, b, c, ...};
```

## 常用内置函数

```c++
vector<T> vec;
vec.push_back();
vec.emplace(..)；
```

